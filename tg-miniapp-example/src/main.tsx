import React from 'react';
import ReactDOM from 'react-dom/client';
import TgMiniAppExample from './TgMiniAppExample.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <TgMiniAppExample />
    </React.StrictMode>,
)
